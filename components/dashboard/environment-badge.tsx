"use client";

import { useEffect, useState } from "react";
import { API_BASE_URL } from "@/lib/api/config";

// Componente removido - ya no se muestra el badge de dev mode
export function EnvironmentBadge() {
  return null;
}
